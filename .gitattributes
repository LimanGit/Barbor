# Auto detect text files and perform LF normalization
* text=auto

# Explicitly declare binary files so Git never touches them
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.webp binary
*.svg  binary

# Also protect other common binaries while you're at it
*.jar  binary
*.zip  binary
*.pdf  binary
